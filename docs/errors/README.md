# Error Catalog

Generated centralized error catalogs point here as the canonical repository-owned documentation
surface for result-error definitions.

The first implementation slice establishes generated error catalog metadata and assembly-level
catalog providers. A later slice can expand this into per-error rendered pages or HTTP
documentation endpoints without changing the generated identifiers, provider types, or entry shape.
