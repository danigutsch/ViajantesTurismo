# SharedKernel.EventSourcing

Storage-neutral event-sourcing primitives for aggregates, append/load operations, stream versions, and projections.

This project intentionally contains abstractions only. Database-specific event stores and projection runners belong in
bounded-context infrastructure projects.
