# SharedKernel.IntegrationEvents.CloudEvents

CloudEvents metadata adapter for typed integration events.

This project keeps transport metadata mapping separate from the core integration event contracts.
